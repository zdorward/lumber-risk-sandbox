## Docker Quickstart

Prereqs:

- Docker installed and running

```bash
git clone <repo-url> lumber-risk-sandbox
cd lumber-risk-sandbox

docker compose up --build
```

---

### How to stop it

From the terminal where Docker is running:

```bash
CTRL+C
docker compose down
```
